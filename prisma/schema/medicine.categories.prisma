model Categories {
    id        String     @unique @default(uuid())
    slug      String
    name      String
    medicines Medicine[]
    sellerId  String
    createdAt DateTime   @default(now())
    updatedAt DateTime   @updatedAt

    @@index([sellerId])
}
