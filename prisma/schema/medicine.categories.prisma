model Categories {
    id        String     @unique @default(uuid())
    slug      String
    name      String
    medicines Medicine[]
    createdAt DateTime   @default(now())
    sellerId  String
    updatedAt DateTime   @updatedAt

    @@index([sellerId])
}
