model Categories {
    id        String     @unique @default(uuid())
    slug      String
    name      String
    medicines Medicine[]
    createdAt DateTime   @default(now())
    updatedAt DateTime   @updatedAt
}
